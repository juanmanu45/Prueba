import { RedisClientOptions, RedisFunctions, RedisModules, RedisScripts } from 'redis';
export declare class RedisConnection {
    private client;
    constructor(clientOptions?: RedisClientOptions);
    connect(): Promise<import("@redis/client").RedisClientType<RedisModules, RedisFunctions, RedisScripts>>;
    disconnect(): Promise<void>;
}
