import { ModelAttributes, ModelOptions, Options, Sequelize } from "@sequelize/core";
export declare interface SequelizeConnectionConfig {
    host: string;
    username: string;
    password: string;
    database: string;
    engine: string;
}
export declare class SequelizeConnection {
    private connection;
    constructor(config: Options);
    connect(): Promise<void>;
    define(name: string, attributes: ModelAttributes, options?: ModelOptions): import("@sequelize/core").ModelStatic<import("@sequelize/core").Model<any, any>>;
    sync(syncOptions?: any): Promise<Sequelize>;
    disconnect(): Promise<void>;
}
