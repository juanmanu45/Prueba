import { NextFunction, Router, RouterOptions, Request, Response } from 'express';
import { APIRequest } from './APIRequest';
export declare abstract class APIRouter {
    path: string;
    router: Router;
    linkedRequest: string[];
    constructor(path: string, options?: RouterOptions);
    init(): void;
    addRequest(request: APIRequest): void;
    protected abstract runMiddleware(req: Request, res: Response, next: NextFunction): Promise<void | Response>;
}
