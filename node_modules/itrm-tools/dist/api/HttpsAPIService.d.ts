/// <reference types="node" />
import { ServerOptions } from 'https';
import { APIService, APIServiceConfig, APIServiceResponse } from "./APIService";
export declare class HttpsAPIService extends APIService {
    private httpsServer;
    private options;
    constructor(config: APIServiceConfig, options: ServerOptions);
    init(): void;
    run(init: Function): Promise<APIServiceResponse>;
}
