import { Response } from 'express';
export declare class ServerSentEventsManager {
    private res;
    constructor(res: Response, config?: any);
    write(status: number, payload: string): void;
    writeObject(status: number, payload: any): void;
    end(status: number, payload: string): void;
}
