import { Request, Response, NextFunction } from "express";
import { APIRequest, APIRequestConfig, RequestMethod } from "../APIRequest";
import { APICheck, Checkable } from "./APIChecker";
export declare abstract class CheckableAPIRequest extends APIRequest implements Checkable {
    private checker;
    constructor(method: RequestMethod, config: APIRequestConfig);
    addCheck(check: APICheck): void;
    check(req: Request, res: Response, next: NextFunction): Promise<void | Response<any, Record<string, any>>>;
}
