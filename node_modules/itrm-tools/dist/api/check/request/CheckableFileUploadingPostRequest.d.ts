import { Express, Router } from "express";
import { APIRequestConfig } from "../../APIRequest";
import { CheckableAPIRequest } from "../CheckableAPIRequest";
import { Multer, StorageEngine } from "multer";
export declare abstract class CheckableFileUploadingPostRequest extends CheckableAPIRequest {
    protected upload: Multer;
    protected storage: StorageEngine;
    constructor(config: APIRequestConfig);
    addToApp(app: Express): void;
    addToRouter(router: Router): void;
}
