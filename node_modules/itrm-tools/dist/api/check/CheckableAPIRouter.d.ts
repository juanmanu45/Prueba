import { RouterOptions, Request, Response, NextFunction } from "express";
import { APIRouter } from "../APIRouter";
import { APICheck, Checkable } from "./APIChecker";
export declare abstract class CheckableAPIRouter extends APIRouter implements Checkable {
    private checker;
    constructor(path: string, options?: RouterOptions);
    init(): void;
    addCheck(checker: APICheck): void;
    check(req: Request, res: Response, next: NextFunction): Promise<void | Response>;
    protected abstract setHeaders(res: Response): void;
}
