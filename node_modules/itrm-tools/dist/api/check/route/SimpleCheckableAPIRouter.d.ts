import { NextFunction, Request, Response, RouterOptions } from "express";
import { CheckableAPIRouter } from "../CheckableAPIRouter";
export declare class SimpleCheckableAPIRouter extends CheckableAPIRouter {
    constructor(path: string, options?: RouterOptions);
    protected setHeaders(res: Response<any, Record<string, any>>): void;
    protected runMiddleware(req: Request, res: Response, next: NextFunction): Promise<void | Response>;
}
