import { Express } from "express";
import cors from "cors";
import { ExpressConfiguration } from "../ExpressConfiguration";
export declare class ExpressStandardConfiguration implements ExpressConfiguration {
    apply(app: Express): void;
    static getCorsConfiguration(): (req: cors.CorsRequest, res: {
        statusCode?: number | undefined;
        setHeader(key: string, value: string): any;
        end(): any;
    }, next: (err?: any) => any) => void;
}
